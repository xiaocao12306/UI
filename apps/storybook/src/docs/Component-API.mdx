import { Meta, Title, Subtitle, Description, Canvas, Controls } from "@storybook/blocks";
import * as ButtonStories from "../stories/Button.stories";
import * as TagStories from "../stories/Tag.stories";
import * as BadgeStories from "../stories/Badge.stories";
import * as InputStories from "../stories/Input.stories";
import * as FormFieldStories from "../stories/FormField.stories";
import * as CheckboxStories from "../stories/Checkbox.stories";
import * as RadioGroupStories from "../stories/RadioGroup.stories";
import * as SwitchStories from "../stories/Switch.stories";
import * as SelectStories from "../stories/Select.stories";
import * as ComboboxStories from "../stories/Combobox.stories";
import * as DatePickerStories from "../stories/DatePicker.stories";
import * as TextareaStories from "../stories/Textarea.stories";
import * as DialogStories from "../stories/Dialog.stories";
import * as DrawerStories from "../stories/Drawer.stories";
import * as TooltipStories from "../stories/Tooltip.stories";
import * as PopoverStories from "../stories/Popover.stories";
import * as DropdownStories from "../stories/Dropdown.stories";
import * as CommandPaletteStories from "../stories/CommandPalette.stories";
import * as EmptyStories from "../stories/Empty.stories";
import * as SkeletonStories from "../stories/Skeleton.stories";
import * as LoadingDotsStories from "../stories/LoadingDots.stories";
import * as StreamingTextStories from "../stories/StreamingText.stories";
import * as ProgressStories from "../stories/Progress.stories";
import * as AlertStories from "../stories/Alert.stories";
import * as ToastStories from "../stories/Toast.stories";
import * as TabsStories from "../stories/Tabs.stories";
import * as TableStories from "../stories/Table.stories";
import * as PaginationStories from "../stories/Pagination.stories";
import * as PromptInputStories from "../stories/PromptInput.stories";
import * as MessageBubbleStories from "../stories/MessageBubble.stories";
import * as ReasoningPanelStories from "../stories/ReasoningPanel.stories";
import * as StreamingCodeBlockStories from "../stories/StreamingCodeBlock.stories";

<Meta title="Docs/Component API" />

<Title />
<Subtitle>Primary API Reference</Subtitle>
<Description>
  This page links the most-used Aurora UI components to executable stories and prop controls. When
  controls are available, Storybook reflects the runtime API contract directly.
</Description>

## Core

### Button

<Canvas of={ButtonStories.Primary} />
<Canvas of={ButtonStories.IconOnlyAccessibleName} />
<Canvas of={ButtonStories.FocusIntentReentry} />
<Controls of={ButtonStories.Primary} />

| Prop                                  | Type                                                                                       | Notes                                                                                                                                                                                            |
| ------------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `variant`                             | `'solid' \| 'ghost' \| 'outline'`                                                          | Semantic visual variants without hardcoded color usage.                                                                                                                                          |
| `size`                                | `'sm' \| 'md' \| 'lg'`                                                                     | Token-aligned button density.                                                                                                                                                                    |
| `loading` / `loadingText`             | `boolean` / `ReactNode`                                                                    | Locks interactions, sets `aria-busy`, and shows deterministic loading copy.                                                                                                                      |
| reduced-motion fallback               | loading spinner tagged with `data-aurora-reduced-motion="animate"`                         | With `GlobalStyles`, spinner animation auto-disables under `prefers-reduced-motion` without changing loading semantics.                                                                          |
| icon-only naming                      | non-empty `aria-label` / `aria-labelledby`                                                 | Recommended on the button itself when visible children are icon-only; rich child content with inline `aria-label` is auto-detected, and development builds warn when naming is still missing.    |
| keyboard press feedback policy        | unmodified `Enter`/`Space` (+ `Space` / legacy `Spacebar` key values) and `Shift` variants | Ignores `Ctrl/Meta/Alt` modified activation chords and IME composition key paths (`isComposing` / `keyCode=229`) so host shortcut combinations and CJK confirmation do not trigger pressed visuals. |
| focus intent policy                   | primary pointer clears focus-visible                                                       | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep focus ring state; plain primary pointer resets keyboard-intent ring before press feedback.                                      |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners                                               | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores button focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| native button props                   | `ButtonHTMLAttributes`                                                                     | Supports `type`, `disabled`, `onClick`, keyboard handlers.                                                                                                                                       |

### Tag

<Canvas of={TagStories.Default} />
<Controls of={TagStories.Default} />

| Prop              | Type                              | Notes                                                  |
| ----------------- | --------------------------------- | ------------------------------------------------------ |
| native span props | `HTMLAttributes<HTMLSpanElement>` | Supports custom `aria-*` metadata and style overrides. |

### Badge

<Canvas of={BadgeStories.Default} />
<Controls of={BadgeStories.Default} />

| Prop              | Type                                 | Notes                                                                |
| ----------------- | ------------------------------------ | -------------------------------------------------------------------- |
| `tone`            | `'default' \| 'success' \| 'danger'` | Semantic status signal without hardcoded app-level palettes.         |
| native span props | `HTMLAttributes<HTMLSpanElement>`    | Supports custom labels and style overrides for compact status chips. |

## Form

### Input

<Canvas of={InputStories.Default} />
<Controls of={InputStories.Default} />

| Prop                                  | Type                                         | Notes                                                                                                                                                                                           |
| ------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| `invalid`                             | `boolean`                                    | Applies `aria-invalid` and error-token border/ring styles; incoming `aria-invalid="grammar                                                                                                      | spelling"` is preserved. |
| `readOnly` / `disabled`               | `boolean`                                    | Preserves semantic behavior and visual state boundaries.                                                                                                                                        |
| `aria-label` / `aria-labelledby`      | `string`                                     | Blank values are ignored; when both are provided, `aria-labelledby` takes precedence so visible context labels remain canonical.                                                                |
| `aria-describedby`                    | `string`                                     | Recommended for helper/error narration in forms.                                                                                                                                                |
| keyboard shortcut hints               | text-like inputs default `aria-keyshortcuts="Enter"` | Text-like actionable inputs (`text/search/email/url/tel/password`) expose Enter shortcut metadata for QA/AT discoverability; non-text types plus read-only/disabled inputs omit default hints (explicit overrides remain supported). |
| Enter active feedback policy          | unmodified `Enter`/`Shift+Enter` only        | Ignores `Ctrl/Meta/Alt + Enter` so host shortcut chords do not trigger input active-press visuals.                                                                                              |
| focus intent policy                   | primary pointer clears focus-visible         | Non-primary `mousedown/pointerdown` plus `Ctrl + primary click` context-click paths keep keyboard-intent focus ring state, while plain primary pointer keeps prior active/pressed behavior.                       |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores input focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| native input props                    | `InputHTMLAttributes`                        | Includes `value`, `defaultValue`, `onChange`, `type`, and autocomplete controls.                                                                                                                |

<Canvas of={InputStories.InvalidTokenSemantics} />
<Canvas of={InputStories.BlankAriaLabelFallback} />
<Canvas of={InputStories.ShortcutHintPrecision} />
<Canvas of={InputStories.FocusIntentReentry} />

### FormField

<Canvas of={FormFieldStories.WithInputControl} />
<Controls of={FormFieldStories.WithInputControl} />

| Prop                             | Type        | Notes                                                                                                                                                                                                                              |
| -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `label`                          | `ReactNode` | Primary accessible name for the wrapped control via generated `htmlFor`.                                                                                                                                                           |
| `description` / `error`          | `ReactNode` | Auto-merges helper and error IDs into child `aria-describedby` contracts.                                                                                                                                                          |
| `required` / `disabled`          | `boolean`   | Field-level semantics merged with child state without dropping existing control props.                                                                                                                                             |
| `children`                       | `ReactNode` | Works with Aurora controls (Input/Select/etc.) and preserves child `aria-*` attributes. Blank child `aria-label` is ignored; when both child `aria-label` and `aria-labelledby` are provided, `aria-labelledby` remains canonical. |
| `htmlFor` for composite controls | `string`    | Required when `children` is not a single clonable control element; development builds warn if label/control association cannot be created.                                                                                         |

<Canvas of={FormFieldStories.InheritedControlSemantics} />
<Canvas of={FormFieldStories.ExternalControlAssociation} />
<Canvas of={FormFieldStories.LabelledByPrecedence} />

### Checkbox

<Canvas of={CheckboxStories.Controlled} />
<Controls of={CheckboxStories.Indeterminate} />

| Prop                                  | Type                                         | Notes                                                                                                                                                                                              |
| ------------------------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `checked` / `defaultChecked`          | `boolean`                                    | Native checkbox controlled/uncontrolled behavior.                                                                                                                                                  |
| `indeterminate`                       | `boolean`                                    | Sets mixed state semantics (`aria-checked="mixed"`).                                                                                                                                               |
| keyboard shortcut hints               | default `aria-keyshortcuts="Space"`          | Actionable checkboxes expose `Space` shortcut metadata for QA/AT discoverability; disabled checkboxes omit hints so metadata never advertises unavailable keyboard actions.                         |
| `description`                         | `ReactNode`                                  | Automatically wires helper text into `aria-describedby`.                                                                                                                                           |
| `aria-label` / `aria-labelledby`      | `string`                                     | Optional explicit naming; blank values are ignored, and `aria-labelledby` takes precedence when both are provided.                                                                                 |
| focus intent policy                   | primary pointer clears focus-visible         | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep checkbox focus-visible state; plain primary pointer clears keyboard-intent focus-visible feedback before toggle intent. |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores checkbox focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| `invalid`                             | `boolean`                                    | Applies invalid state with `aria-invalid`.                                                                                                                                                         |

<Canvas of={CheckboxStories.InvalidAndDisabled} />
<Canvas of={CheckboxStories.ShortcutHintPrecision} />
<Canvas of={CheckboxStories.LabelledByPrecedence} />
<Canvas of={CheckboxStories.FocusIntentReentry} />

### RadioGroup

<Canvas of={RadioGroupStories.Controlled} />
<Controls of={RadioGroupStories.DisabledOption} />

| Prop                                  | Type                                         | Notes                                                                                                                                                                                           |
| ------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `options`                             | `RadioOption[]`                              | Defines each choice label/value pair. Development builds warn when option values are duplicated; runtime render keys are auto-suffixed to avoid React key collisions, but values should stay unique. |
| `value` / `defaultValue`              | `string`                                     | Controlled and uncontrolled single-choice flows.                                                                                                                                                |
| `ariaLabel` / `ariaLabelledBy`        | `string` / `string`                          | Optional radiogroup naming override; blank values are ignored, and `ariaLabelledBy` takes precedence when both are provided (`ariaLabel` otherwise falls back to `name`).                       |
| `options[].disabled`                  | `boolean`                                    | Disable specific choices without removing context.                                                                                                                                              |
| option shortcut hints                 | actionable options expose `aria-keyshortcuts="Space"` | Keyboard-discoverability metadata is attached only to actionable radio options; disabled options omit hints so QA/AT metadata does not advertise unavailable activation paths.                  |
| focus intent policy                   | primary pointer clears focus-visible         | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep focused-option focus-visible state; plain primary pointer clears keyboard-intent focus-visible feedback before selection intent. |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores radio focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| `invalid`                             | `boolean`                                    | Applies `aria-invalid` to the `radiogroup` container for screen-reader narration.                                                                                                               |
| `direction`                           | `'vertical' \| 'horizontal'`                 | Layout direction for stacked vs inline groups; also maps to `aria-orientation` on the `radiogroup` container.                                                                                 |

<Canvas of={RadioGroupStories.InvalidState} />
<Canvas of={RadioGroupStories.ShortcutHintPrecision} />
<Canvas of={RadioGroupStories.LabelledByPrecedence} />
<Canvas of={RadioGroupStories.FocusIntentReentry} />

### Switch

<Canvas of={SwitchStories.Controlled} />
<Controls of={SwitchStories.KeyboardToggle} />

| Prop                                  | Type                                            | Notes                                                                                                                                                                                            |
| ------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `checked` / `defaultChecked`          | `boolean`                                       | Controlled and uncontrolled switch flows.                                                                                                                                                        |
| `onCheckedChange`                     | `(checked: boolean) => void`                    | Triggered from pointer and keyboard activation (`Space`, `Spacebar`, ` `).                                                                                                                       |
| keyboard shortcut hints               | default `aria-keyshortcuts="Space"`             | Actionable switches expose `Space` shortcut metadata for QA/AT discoverability; disabled switches omit hints so metadata never advertises unavailable keyboard actions.                           |
| keyboard pressed feedback policy      | unmodified `Space` (` ` / `Space` / `Spacebar`) | Switch exposes `data-pressed` only while an unmodified activation key is held; `Ctrl/Meta/Alt` chords and IME composition paths (`isComposing` / `keyCode=229`) stay passive to avoid false feedback. |
| click guard                           | `onClick(event.preventDefault())`               | Pointer toggle respects consumer click guards; blocked clicks do not emit `onCheckedChange`.                                                                                                     |
| `description`                         | `ReactNode`                                     | Auto-wires helper copy to `aria-describedby`.                                                                                                                                                    |
| `aria-label` / `aria-labelledby`      | `string`                                        | Optional explicit naming; blank string values are ignored, and `aria-labelledby` takes precedence when both are provided.                                                                        |
| focus intent + pressed policy         | plain primary pointer only                      | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep switch focus-visible state and do not trigger `data-pressed`; plain primary pointer clears focus-visible and can set pressed feedback. |
| keyboard modifier guard               | ignores `Ctrl/Meta/Alt + Space` switch handling | Preserves host/browser shortcut chords and avoids accidental toggle from modified key combinations.                                                                                              |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners    | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores switch focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| `invalid`                             | `boolean`                                       | Applies invalid semantics and error-token visual states.                                                                                                                                         |

<Canvas of={SwitchStories.InvalidAndDisabled} />
<Canvas of={SwitchStories.ShortcutHintPrecision} />
<Canvas of={SwitchStories.KeyboardModifierGuard} />
<Canvas of={SwitchStories.ImeCompositionGuard} />
<Canvas of={SwitchStories.LabelledByPrecedence} />
<Canvas of={SwitchStories.ClickPreventDefaultGuard} />
<Canvas of={SwitchStories.FocusIntentReentry} />

### Select

<Canvas of={SelectStories.Controlled} />
<Controls of={SelectStories.DisabledState} />

| Prop                                  | Type                                         | Notes                                                                                                                                                                                            |
| ------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `value` / `defaultValue`              | `string`                                     | Supports controlled and uncontrolled native select flows.                                                                                                                                        |
| option value diagnostics              | development duplicate-value warning          | Development builds warn when rendered `<option value>` values are duplicated so native selected-option semantics remain deterministic.                                                            |
| `invalid`                             | `boolean`                                    | Adds `aria-invalid` and error-token border/ring semantics.                                                                                                                                       |
| `disabled`                            | `boolean`                                    | Applies disabled semantics and visual de-emphasis.                                                                                                                                               |
| keyboard shortcut hints               | default `aria-keyshortcuts="ArrowDown ArrowUp"` | Actionable selects expose ArrowDown/ArrowUp shortcut metadata for QA/AT discoverability; disabled selects omit hints so metadata does not advertise unavailable interaction paths.               |
| `aria-label` / `aria-labelledby`      | `string`                                     | Blank values are ignored; when both are provided, `aria-labelledby` takes precedence so visible external headings remain canonical.                                                              |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores select focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |

<Canvas of={SelectStories.InvalidState} />
<Canvas of={SelectStories.BlankAriaLabelFallback} />
<Canvas of={SelectStories.LabelledByPrecedence} />
<Canvas of={SelectStories.ShortcutHintPrecision} />
<Canvas of={SelectStories.FocusIntentReentry} />

### Combobox

<Canvas of={ComboboxStories.Controlled} />
<Controls of={ComboboxStories.WithCustomEmptyMessage} />

| Prop                                  | Type                                                                    | Notes                                                                                                                                                                                                              |
| ------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `options`                             | `ComboboxOption[]`                                                      | Supports disabled entries and keyword filtering. Development builds warn when option values are duplicated; runtime render keys are auto-suffixed to avoid React key collisions, but values should still stay unique. |
| `value` / `defaultValue`              | `string`                                                                | Controlled and uncontrolled modes are both available.                                                                                                                                                              |
| `onValueChange`                       | `(value: string) => void`                                               | Fires when user confirms option by click or Enter.                                                                                                                                                                 |
| `ariaLabel` / `ariaLabelledBy`       | `string` / `string`                                                     | Optional combobox/listbox naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without `ariaLabelledBy`, `ariaLabel` falls back to `Combobox` and listbox naming follows `\`${ariaLabel} options\``. |
| `emptyMessage`                        | `ReactNode`                                                             | Custom no-result content for search miss scenarios.                                                                                                                                                                |
| trigger relation                      | conditional `aria-controls`                                             | Input references listbox ID only while results listbox is mounted.                                                                                                                                                 |
| option focus model                    | `aria-activedescendant` + `tabIndex=-1` options                         | Keyboard focus stays on combobox input; pointer selection uses primary-pointer `mousedown` guard to avoid focus theft while ignoring `Ctrl + primary click` context-click paths.                                   |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners                            | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores combobox focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| modifier-key guard                    | ignores `Ctrl/Meta/Alt + Arrow/Home/End/Enter/Escape` combobox handling | Preserves host/browser modified-key shortcuts and avoids accidental combobox navigation, selection, or dismiss from combined key chords.                                                                           |
| IME safety                            | composition key guard                                                   | Arrow/Home/End/Enter/Escape handling is ignored while `isComposing` and legacy fallback paths that only expose `keyCode=229`, so CJK confirmation does not trigger navigation, selection, or dismiss unexpectedly. |
| accent-insensitive search             | NFD + diacritic strip + whitespace normalization                         | Query matching ignores accent marks and repeated whitespace so plain-text input (for example `cafe`) still matches localized options/keywords such as `Café` / `déploiement`.                                      |
| keyboard shortcut hints               | `aria-keyshortcuts` state-aware metadata                                | Closed-state combobox keeps baseline `Enter` hint inherited from text-input behavior; expanded-state metadata adds navigation hints only when multiple enabled options exist and shows `Escape` only while popup is open. |
| dismiss policy                        | `Escape` / blur outside                                                 | Keeps list open when focus moves into option elements; closes on blur outside root.                                                                                                                                |
| outside pointer policy                | primary pointer only + preemption guard                                 | Outside `pointerdown` close ignores non-primary mouse buttons and `Ctrl + primary` context-click paths, and skips close when upstream handlers call `preventDefault()`.                                             |

<Canvas of={ComboboxStories.KeyboardDismissPaths} />
<Canvas of={ComboboxStories.ModifierKeyGuard} />
<Canvas of={ComboboxStories.ImeCompositionGuard} />
<Canvas of={ComboboxStories.BlankAriaLabelFallback} />
<Canvas of={ComboboxStories.LabelledByNameFallback} />
<Canvas of={ComboboxStories.ActivedescendantFocusModel} />
<Canvas of={ComboboxStories.FocusIntentReentry} />
<Canvas of={ComboboxStories.SearchNormalizationAndShortcutHints} />

### DatePicker

<Canvas of={DatePickerStories.Controlled} />
<Controls of={DatePickerStories.StateMatrix} />

| Prop                                  | Type                                         | Notes                                                                                                                                                                                                |
| ------------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `value` / `defaultValue`              | `string`                                     | Uses native `YYYY-MM-DD` value format.                                                                                                                                                               |
| `onValueChange`                       | `(value: string) => void`                    | Emits selected date string from native date input.                                                                                                                                                   |
| `onChange`                            | `(event) => void`                            | Optional native change callback for analytics/instrumentation hooks.                                                                                                                                 |
| `min` / `max`                         | `string`                                     | Native date range constraints.                                                                                                                                                                       |
| `invalid`                             | `boolean`                                    | Adds `aria-invalid` to communicate validation state.                                                                                                                                                 |
| `aria-label` / `aria-labelledby`      | `string`                                     | Blank values are ignored; when both naming props are provided, `aria-labelledby` remains canonical.                                                                                                  |
| keyboard shortcut hints               | no default hint for `type="date"`            | DatePicker intentionally omits default Enter shortcut metadata because it maps to a non-text native date input; explicit `aria-keyshortcuts` overrides remain supported for product-specific flows. |
| `errorMessageId`                      | `string`                                     | Appends error helper ID into `aria-describedby` and `aria-errormessage` when `invalid` is true (deduped if ID already exists).                                                                       |
| focus intent policy                   | primary pointer clears focus-visible         | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep date input focus-visible state; plain primary pointer clears keyboard-intent focus-visible feedback before selection intent. |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores date input focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |

<Canvas of={DatePickerStories.InvalidWithHelper} />
<Canvas of={DatePickerStories.StateMatrix} />
<Canvas of={DatePickerStories.ShortcutHintPrecision} />
<Canvas of={DatePickerStories.BlankAriaLabelFallback} />
<Canvas of={DatePickerStories.LabelledByPrecedence} />
<Canvas of={DatePickerStories.FocusIntentReentry} />

### Textarea

<Canvas of={TextareaStories.Default} />
<Controls of={TextareaStories.Default} />

| Prop                                  | Type                                         | Notes                                                                                                                                                                                              |
| ------------------------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `invalid`                             | `boolean`                                    | Adds `aria-invalid` and invalid token styles.                                                                                                                                                      |
| `readOnly` / `disabled`               | `boolean`                                    | Preserves semantic behavior and visual state boundaries.                                                                                                                                           |
| `aria-label` / `aria-labelledby`      | `string`                                     | Blank values are ignored; when both are provided, `aria-labelledby` takes precedence so visible context headings remain canonical.                                                                 |
| `aria-describedby`                    | `string`                                     | Supports helper/error hint linkage for form narration.                                                                                                                                             |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores textarea focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |

<Canvas of={TextareaStories.InvalidWithHelper} />
<Canvas of={TextareaStories.BlankAriaLabelFallback} />
<Canvas of={TextareaStories.LabelledByPrecedence} />
<Canvas of={TextareaStories.FocusIntentReentry} />

## Overlay

### Tooltip

<Canvas of={TooltipStories.Default} />
<Controls of={TooltipStories.Default} />

| Prop                           | Type                                                                                           | Notes                                                                                                                               |
| ------------------------------ | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `open` / `defaultOpen`         | `boolean`                                                                                      | Supports controlled and uncontrolled visibility flow.                                                                               |
| `onOpenChange`                 | `(open: boolean) => void`                                                                      | Emits open-state transitions across pointer and keyboard interactions.                                                              |
| `delayDuration` / `closeDelay` | `number`                                                                                       | Fine-grained pointer open/close timing control.                                                                                     |
| `disabled`                     | `boolean`                                                                                      | Prevents open behavior and closes active tooltip state.                                                                             |
| `closeOnEscape`                | `boolean`                                                                                      | Controls whether Escape dismiss is enabled; disabling it keeps tooltip open on Escape and removes dismiss shortcut metadata.        |
| `side` / `sideOffset`          | `'top' \| 'right' \| 'bottom' \| 'left'` / `number`                                            | Controls overlay placement around trigger.                                                                                          |
| dismiss key hints              | `aria-keyshortcuts="Escape"` (while visible and `closeOnEscape` enabled)                       | Tooltip surface exposes Escape shortcut metadata for QA/AT discoverability only when Escape dismiss is actionable.                  |
| modifier/repeat guard          | ignores `Ctrl/Meta/Alt + Escape`, repeated keydown, and IME composition Escape (`keyCode=229`) | Preserves host/browser shortcuts and avoids accidental tooltip dismissal while handling long-press or CJK composition confirmation. |

<Canvas of={TooltipStories.Controlled} />
<Canvas of={TooltipStories.DescribedByLifecycle} />
<Canvas of={TooltipStories.EscapeModifierGuard} />
<Canvas of={TooltipStories.EscapeDisabled} />

### Dialog

<Canvas of={DialogStories.Interactive} />
<Controls of={DialogStories.Interactive} />

| Prop                                       | Type                                                                                                                        | Notes                                                                                                                                                                                                                    |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `open` / `onOpenChange`                    | `boolean` / `(open: boolean) => void`                                                                                       | Controlled visibility contract for overlays.                                                                                                                                                                             |
| `title` / `description`                    | `ReactNode`                                                                                                                 | Wires `aria-labelledby` + optional `aria-describedby`.                                                                                                                                                                   |
| `ariaLabel` / `ariaLabelledBy`             | `string` / `string`                                                                                                         | Optional dialog naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without overrides, dialog naming falls back to rendered `title`.                                                   |
| `size`                                     | `'sm' \| 'md' \| 'lg'`                                                                                                      | Overlay width presets.                                                                                                                                                                                                   |
| `restoreFocus`                             | `boolean`                                                                                                                   | Whether to restore focus to the previously focused element when dialog closes.                                                                                                                                           |
| `closeOnEscape` / `closeOnOutsidePointer`  | `boolean`                                                                                                                   | Controls dismiss policy for blocking vs non-blocking dialogs.                                                                                                                                                            |
| `showCloseButton` / `closeLabel`           | `boolean` / `string`                                                                                                        | Close affordance visibility and accessible label text; blank `closeLabel` values are ignored and fallback to `Close dialog`.                                                                                             |
| `onEscapeKeyDown` / `onPointerDownOutside` | event callbacks                                                                                                             | Inspect or cancel dismiss intent before `onOpenChange(false)`.                                                                                                                                                           |
| `onCloseReason`                            | `(reason: 'close-button' \| 'escape-key' \| 'outside-pointer') => void`                                                     | Emits structured close telemetry for dialog-dismiss analytics and workflow tracing.                                                                                                                                      |
| close lifecycle order                      | `onCloseReason -> onOpenChange(false)`                                                                                      | Close-button/Escape/outside-pointer paths use deterministic callback order for telemetry and reducer side effects.                                                                                                       |
| dismiss-event preemption                   | handled Escape/outside-pointer calls `event.preventDefault()`                                                               | Dialog consumes handled dismiss events so downstream global handlers can detect preempted dismiss intent (`defaultPrevented=true`) and avoid duplicate close side effects.                                               |
| close-button focus intent                  | primary pointer clears focus-visible                                                                                        | Non-primary close-button pointer down and `Ctrl + primary click` context-click paths keep keyboard-intent ring state to avoid accidental ring loss during context/right-click interactions.                              |
| document-scoped focus intent fallback      | owner-document `keydown` / pointer listeners                                                                                | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from a preceding control) still restores dialog close-button focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| focus intent modifier guard                | owner-document `keydown` ignores `Ctrl/Meta/Alt` chords                                                                     | Modified key chords do not re-enable close-button keyboard-intent fallback focus rings, preventing host shortcut paths from rendering false focus-visible affordances.                                                     |
| dismiss key hints                          | `aria-keyshortcuts="Escape"` when `closeOnEscape` is enabled                                                                | Dialog surface exposes Escape shortcut metadata for QA/AT discoverability only when Escape dismiss is actionable; disabled Escape policy omits the hint.                                                                 |
| close-button pressed feedback              | primary pointer + unmodified `Enter`/`Space` (+ `Space` / legacy `Spacebar` key values) + `aria-keyshortcuts="Enter Space"` | Close button exposes pressed offset/background on plain primary pointer down and keyboard activation key hold with explicit shortcut hints; non-primary pointer buttons, `Ctrl + primary click` context-click paths, `Ctrl/Meta/Alt` chords, and IME composition key paths (`isComposing` / `keyCode=229`) are ignored so host shortcuts and CJK confirmation do not flash pressed visuals. |
| focus trap cycle                           | `Tab` / `Shift+Tab` loop inside dialog                                                                                      | Modal focus scope keeps keyboard focus inside dialog controls while open.                                                                                                                                                |
| IME Escape safety                          | composition guard                                                                                                           | Escape dismiss is ignored during active IME composition (`isComposing` / `keyCode=229`) to prevent accidental close while confirming CJK input.                                                                          |
| modifier-key guard                         | ignores `Ctrl/Meta/Alt + Escape`                                                                                            | Preserves host/browser modified-key shortcuts and avoids accidental dialog dismiss/hook side effects from combined key chords.                                                                                           |
| Escape repeat guard                        | ignores repeated keydown                                                                                                    | Long-press Escape (`event.repeat=true`) is ignored so one key press closes at most one dialog and avoids duplicate dismiss telemetry.                                                                                    |
| Escape priority                            | preempted events skip dialog hook/dismiss                                                                                   | If higher-level handlers already `preventDefault()` Escape, dialog stays open and does not fire `onEscapeKeyDown`.                                                                                                       |
| nested dismiss order                       | top layer first                                                                                                             | Nested overlay stacks (for example dropdown inside dialog) dismiss one layer per Escape/outside interaction.                                                                                                             |
| portal host scope                          | `ownerDocument.body` auto-target                                                                                            | Dialog portal mounts to the invoking document root (including iframe/multi-root hosts) instead of always using top-level `document.body`.                                                                                |

<Canvas of={DialogStories.NestedDismissOrder} />
<Canvas of={DialogStories.DismissGuardHooks} />
<Canvas of={DialogStories.CloseReasonTelemetry} />
<Canvas of={DialogStories.LabelledByPrecedence} />
<Canvas of={DialogStories.FocusTrapKeyboardCycle} />
<Canvas of={DialogStories.FocusIntentReentry} />
<Canvas of={DialogStories.CloseButtonKeyboardPressedState} />
<Canvas of={DialogStories.EscapeIgnoresImeComposition} />
<Canvas of={DialogStories.EscapePreemptedByGlobalHandler} />
<Canvas of={DialogStories.EscapeRepeatGuard} />

### Drawer

<Canvas of={DrawerStories.Interactive} />
<Controls of={DrawerStories.NonDismissible} />

| Prop                                      | Type                                                                                                                        | Notes                                                                                                                                                                                                                    |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `side`                                    | `'left' \| 'right'`                                                                                                         | Controls panel placement and border direction.                                                                                                                                                                           |
| `description`                             | `ReactNode`                                                                                                                 | Wires helper copy through `aria-describedby`.                                                                                                                                                                            |
| `ariaLabel` / `ariaLabelledBy`            | `string` / `string`                                                                                                         | Optional drawer naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without overrides, drawer naming falls back to rendered `title`.                                             |
| `restoreFocus`                            | `boolean`                                                                                                                   | Controls whether focus returns to previously focused element when drawer closes.                                                                                                                                         |
| `closeOnEscape` / `closeOnOutsidePointer` | `boolean`                                                                                                                   | Fine-grained dismiss policy for blocking flows.                                                                                                                                                                          |
| `onCloseReason`                           | `(reason: 'close-button' \| 'escape-key' \| 'outside-pointer') => void`                                                     | Emits structured close telemetry for close-button vs dismiss-gesture analytics.                                                                                                                                          |
| close lifecycle order                     | `onCloseReason -> onOpenChange(false)`                                                                                      | Close-button/Escape/outside-pointer paths keep callback order deterministic for telemetry reducers.                                                                                                                      |
| dismiss-event preemption                  | handled Escape/outside-pointer calls `event.preventDefault()`                                                               | Drawer consumes handled dismiss events so downstream global handlers can detect preempted dismiss intent (`defaultPrevented=true`) and avoid duplicate close side effects.                                               |
| `showCloseButton`                         | `boolean`                                                                                                                   | Allows explicit in-panel action-only dismissal.                                                                                                                                                                          |
| `closeLabel`                              | `string`                                                                                                                    | Localizable close-button accessible name; blank values are ignored and fallback to `Close drawer`.                                                                                                                       |
| dismiss key hints                         | `aria-keyshortcuts="Escape"` when `closeOnEscape` is enabled                                                                | Drawer surface exposes Escape shortcut metadata for QA/AT discoverability only when Escape dismiss is actionable; disabled Escape policy omits the hint.                                                                 |
| close-button focus intent                 | primary pointer clears focus-visible                                                                                        | Non-primary close-button pointer down and `Ctrl + primary click` context-click paths keep keyboard-intent ring state to avoid accidental ring loss during context/right-click interactions.                              |
| document-scoped focus intent fallback     | owner-document `keydown` / pointer listeners                                                                                | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from a preceding control) still restores drawer close-button focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| focus intent modifier guard               | owner-document `keydown` ignores `Ctrl/Meta/Alt` chords                                                                     | Modified key chords do not re-enable close-button keyboard-intent fallback focus rings, preventing host shortcut paths from rendering false focus-visible affordances.                                                     |
| close-button pressed feedback             | primary pointer + unmodified `Enter`/`Space` (+ `Space` / legacy `Spacebar` key values) + `aria-keyshortcuts="Enter Space"` | Close button exposes pressed offset/background on plain primary pointer down and keyboard activation key hold with explicit shortcut hints; non-primary pointer buttons, `Ctrl + primary click` context-click paths, `Ctrl/Meta/Alt` chords, and IME composition key paths (`isComposing` / `keyCode=229`) are ignored so host shortcuts and CJK confirmation do not flash pressed visuals. |
| focus trap cycle                          | `Tab` / `Shift+Tab` loop inside drawer                                                                                      | Modal focus scope keeps keyboard focus inside drawer controls while open.                                                                                                                                                |
| modifier-key guard                        | ignores `Ctrl/Meta/Alt + Escape`                                                                                            | Preserves host/browser modified-key shortcuts and avoids accidental drawer dismiss/hook side effects from combined key chords.                                                                                           |
| Escape repeat guard                       | ignores repeated keydown                                                                                                    | Long-press Escape (`event.repeat=true`) is ignored so one key press closes at most one drawer and avoids duplicate dismiss telemetry.                                                                                    |
| Escape priority                           | preempted events skip drawer hook/dismiss                                                                                   | If higher-level handlers already `preventDefault()` Escape, drawer stays open and does not fire `onEscapeKeyDown`.                                                                                                       |
| nested dismiss order                      | top layer first                                                                                                             | Nested overlays inside drawer dismiss one layer per Escape/outside interaction.                                                                                                                                          |
| portal host scope                         | `ownerDocument.body` auto-target                                                                                            | Drawer portal mounts to the invoking document root (including iframe/multi-root hosts) instead of always using top-level `document.body`.                                                                                |

<Canvas of={DrawerStories.FocusReturn} />
<Canvas of={DrawerStories.NoFocusReturn} />
<Canvas of={DrawerStories.NestedDismissOrder} />
<Canvas of={DrawerStories.DismissGuardHooks} />
<Canvas of={DrawerStories.CloseReasonTelemetry} />
<Canvas of={DrawerStories.LabelledByPrecedence} />
<Canvas of={DrawerStories.FocusTrapKeyboardCycle} />
<Canvas of={DrawerStories.FocusIntentReentry} />
<Canvas of={DrawerStories.CloseButtonKeyboardPressedState} />
<Canvas of={DrawerStories.EscapeIgnoresImeComposition} />
<Canvas of={DrawerStories.EscapePreemptedByGlobalHandler} />
<Canvas of={DrawerStories.EscapeRepeatGuard} />

### Popover

<Canvas of={PopoverStories.WithInteractiveContent} />
<Controls of={PopoverStories.Default} />

| Prop                                       | Type                                                                                  | Notes                                                                                                                                                                                             |
| ------------------------------------------ | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `triggerLabel`                             | `ReactNode`                                                                           | Trigger button content.                                                                                                                                                                           |
| `triggerAriaLabel` / `triggerAriaLabelledBy` | `string` / `string`                                                                   | Accessible naming controls for icon-only trigger content. Blank values are ignored; when both are provided, `triggerAriaLabelledBy` takes precedence and suppresses `triggerAriaLabel` fallback. |
| `open` / `defaultOpen` / `onOpenChange`    | controlled/uncontrolled                                                               | Supports both managed and unmanaged overlay state.                                                                                                                                                |
| `closeOnEscape` / `closeOnOutsidePointer`  | `boolean`                                                                             | Configures whether keyboard/pointer dismiss gestures are allowed for non-blocking vs blocking workflows.                                                                                          |
| `onEscapeKeyDown` / `onPointerDownOutside` | event callbacks                                                                       | Observe or cancel dismiss intents before close policy resolves.                                                                                                                                   |
| `onCloseReason`                            | `(reason: 'trigger-click' \| 'escape-key' \| 'outside-pointer' \| 'tab-key') => void` | Emits structured close telemetry for trigger-toggle vs dismiss-gesture analytics.                                                                                                                 |
| close lifecycle order                      | `onCloseReason -> onOpenChange(false)`                                                | Trigger/Escape/outside-pointer/Tab dismiss paths share deterministic close callback order for telemetry reducers.                                                                                 |
| `align` / `sideOffset`                     | `'start' \| 'end'` / `number`                                                         | Horizontal alignment and vertical spacing from trigger.                                                                                                                                           |
| `contentLabel`                             | `string`                                                                              | Accessible name for popover dialog content region; blank values are ignored and fallback to `Popover content`.                                                                                    |
| trigger relation                           | conditional `aria-controls`                                                           | Trigger references content ID only while open to avoid dangling ARIA links.                                                                                                                       |
| dismiss focus policy                       | Escape restores trigger focus; outside pointer keeps target focus                     | Prevents keyboard dead-ends without stealing pointer intent.                                                                                                                                      |
| keyboard boundary dismiss                  | `Tab` -> next control, `Shift+Tab` -> previous control                                | Popover stays open while tabbing within focusable content, then closes at first/last boundary and transfers focus to adjacent document controls in order.                                         |
| nested dismiss order                       | top layer first                                                                       | Nested popovers dismiss one layer per Escape/outside interaction so parent overlays remain stable until directly dismissed.                                                                       |
| IME Escape safety                          | composition guard                                                                     | Escape dismiss is ignored during active IME composition (`isComposing`) and legacy fallback paths that only expose `keyCode=229`, so CJK confirmation does not collapse the popover unexpectedly. |
| modifier-key guard                         | ignores `Ctrl/Meta/Alt + Escape`                                                      | Preserves host/browser modified-key shortcuts and avoids accidental popover dismiss/hook side effects from combined key chords.                                                                   |
| dismiss key hints                          | `aria-keyshortcuts="Tab [Escape]"`                                                    | Popover surface always exposes boundary Tab-dismiss hints; `Escape` hint appears only when `closeOnEscape` is enabled so shortcut metadata matches runtime policy.                                |
| trigger keyboard-open guard                | ignores `Ctrl/Meta/Alt + ArrowDown`                                                   | Trigger keyboard open shortcut responds only to unmodified `ArrowDown`, preserving host/browser shortcut chords.                                                                                  |
| trigger keyboard key hints                 | `aria-keyshortcuts="ArrowDown"` while closed                                          | Trigger exposes keyboard-open shortcut metadata for QA/AT discoverability when open action is available, and omits it while popover is already open.                                              |
| Escape repeat guard                        | ignores repeated keydown                                                              | Long-press Escape (`event.repeat=true`) is ignored so one key press closes at most one popover and avoids duplicate dismiss telemetry.                                                            |
| Escape priority                            | preempted events skip popover hook/dismiss                                            | If higher-level handlers already `preventDefault()` Escape, popover stays open and does not fire `onEscapeKeyDown`.                                                                               |
| dismiss event scope                        | `ownerDocument`-scoped layer listeners                                                | Escape/outside-pointer dismiss responds only to events from the popover document root; cross-document events are ignored, and `Ctrl + primary` context-click pointer paths do not dismiss.       |

<Canvas of={PopoverStories.NonDismissible} />
<Canvas of={PopoverStories.DismissGuardHooks} />
<Canvas of={PopoverStories.NestedDismissOrder} />
<Canvas of={PopoverStories.NestedOutsideDismissOrder} />
<Canvas of={PopoverStories.OutsideDismissFocusTransfer} />
<Canvas of={PopoverStories.TabDismissToNextControl} />
<Canvas of={PopoverStories.ShiftTabDismissToPreviousControl} />
<Canvas of={PopoverStories.CloseReasonTelemetry} />
<Canvas of={PopoverStories.TriggerLabelledByPrecedence} />
<Canvas of={PopoverStories.TriggerArrowDownModifierGuard} />
<Canvas of={PopoverStories.EscapePreemptedByGlobalHandler} />
<Canvas of={PopoverStories.EscapeRepeatGuard} />
<Canvas of={PopoverStories.EscapeIgnoresImeComposition} />

### Dropdown

<Canvas of={DropdownStories.Default} />
<Controls of={DropdownStories.Default} />

| Prop                                       | Type                                                                                                                         | Notes                                                                                                                                                                                                                                                    |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `items`                                    | `DropdownItem[]`                                                                                                             | Each item supports `disabled` and `onSelect`. Development builds warn when item keys are duplicated, and duplicate render keys are auto-suffixed by item index to avoid React key-collision remount churn.                                                                                                      |
| `items[].ariaLabel`                        | `string`                                                                                                                     | Accessible name override for non-text/icon-only menu items; rich labels with inline `aria-label` are auto-detected, and development builds warn when non-text labels omit both.                                                                          |
| `items[].textValue`                        | `string`                                                                                                                     | Typeahead/search alias for non-text labels or locale-specific/accent-normalized matching; rich labels with readable text or inline `aria-label` are auto-detected before warning.                                                                        |
| `triggerAriaLabel` / `triggerAriaLabelledBy` | `string` / `string`                                                                                                          | Accessible naming controls for icon-only trigger button content. Blank values are ignored; when both are provided, `triggerAriaLabelledBy` takes precedence and suppresses `triggerAriaLabel` fallback.                                                                                               |
| `open` / `defaultOpen` / `onOpenChange`    | controlled/uncontrolled                                                                                                      | Works in both controlled and uncontrolled menu flows.                                                                                                                                                                                                    |
| controlled close contract                  | close intent first, parent rerender second                                                                                   | In controlled mode, dismiss gestures emit close callbacks first; menu visibility changes only after parent updates `open`.                                                                                                                               |
| `closeOnEscape` / `closeOnOutsidePointer`  | `boolean`                                                                                                                    | Allows keeping menu open on Escape/outside pointer for blocking approval flows.                                                                                                                                                                          |
| `onEscapeKeyDown` / `onPointerDownOutside` | event callbacks                                                                                                              | Observe or cancel dismiss gestures before close policy resolves.                                                                                                                                                                                         |
| `onCloseReason`                            | `(reason: 'trigger-click' \| 'item-select' \| 'escape-key' \| 'outside-pointer' \| 'tab-key') => void`                       | Emits structured close telemetry so analytics does not infer dismiss origin from side effects.                                                                                                                                                           |
| close lifecycle order                      | `item-select: onSelect -> onCloseReason -> onOpenChange(false)`; other dismiss paths: `onCloseReason -> onOpenChange(false)` | Keep action side effects and close telemetry deterministic across menu interactions.                                                                                                                                                                     |
| menu naming                                | trigger-linked `aria-labelledby`                                                                                             | Menu inherits an accessible name from trigger content for screen-reader context.                                                                                                                                                                         |
| trigger relation                           | conditional `aria-controls`                                                                                                  | Trigger points to menu ID only when expanded.                                                                                                                                                                                                            |
| trigger keyboard-open targeting            | `ArrowDown` -> first enabled item, `ArrowUp` -> last enabled item                                                            | Trigger keyboard open path is deterministic in both directions so users can jump to top or tail actions without additional arrow traversal.                                                                                                              |
| keyboard behavior                          | Arrow / Home / End / PageUp / PageDown / Enter / Space / Escape / Tab / Typeahead                                            | Roving-focus menu navigation supports non-pointer item selection (Enter/Space incl. legacy `Spacebar`), long-list page jumps, deterministic close policy, buffered multi-character typeahead, repeated-key cycling, and trigger focus restore on Escape. |
| tab dismiss focus transfer                 | `Tab` -> next control, `Shift+Tab` -> previous control                                                                       | Menu closes on `Tab`/`Shift+Tab` and transfers focus to adjacent controls in document order, including iframe ownerDocument hosts, so keyboard continuation remains deterministic without focus loss.                                                    |
| menu keyboard key hints                    | `aria-keyshortcuts="[ArrowDown ArrowUp Home End PageDown PageUp] Tab [Escape]"`                                              | Menu surface always exposes `Tab` dismiss-continuation hints, includes `Escape` only when `closeOnEscape={true}`, and adds navigation hints only when more than one menu item is actionable so shortcut metadata does not advertise unavailable movement paths. |
| modifier-key guard                         | ignores `Ctrl/Meta/Alt + Arrow/Home/End/PageUp/PageDown/Enter/Space/Escape`                                                  | Preserves host app/browser modified-key shortcuts and avoids accidental menu navigation, action execution, or dismiss hook side effects under combined shortcut chords, including open-menu `ArrowDown/ArrowUp` focus drift.                             |
| trigger keyboard key hints                 | `aria-keyshortcuts="ArrowDown ArrowUp"` while closed                                                                         | Trigger exposes keyboard-open shortcut metadata for QA/AT discoverability when open action is available, and omits it while menu is already open.                                                                                                        |
| keyboard key hints                         | `aria-keyshortcuts="Enter Space"`                                                                                            | Enabled menu items expose activation-key metadata for QA/AT discoverability; disabled items omit shortcut hints.                                                                                                                                         |
| item activation repeat guard               | ignores repeated keydown                                                                                                     | Item selection ignores repeated activation keydown (`Enter` / `Space`, `event.repeat=true`) so long-press does not dispatch duplicate actions or telemetry.                                                                                              |
| keyboard-click dedupe                      | ignores synthesized keyboard-origin click (`detail=0`) after handled activation keydown                                      | Prevents duplicate item action execution and close telemetry when environments emit both keydown and follow-up click for the same keyboard activation.                                                                                                   |
| IME safety                                 | composition key guard                                                                                                        | Typeahead matching is paused during active IME composition (`isComposing`) and legacy fallback paths that only expose `keyCode=229`, so CJK input confirmation does not move focus unexpectedly.                                                         |
| Escape repeat guard                        | ignores repeated keydown                                                                                                     | Long-press Escape (`event.repeat=true`) is ignored so one key press closes at most one dropdown and avoids duplicate dismiss telemetry.                                                                                                                  |
| Escape priority                            | preempted events skip dropdown hook/dismiss                                                                                  | If higher-level handlers already `preventDefault()` Escape, dropdown keeps menu open and does not fire `onEscapeKeyDown`.                                                                                                                                |
| overlay IME Escape safety                  | dismiss-layer composition guard                                                                                              | Escape dismiss is ignored during active IME composition to avoid collapsing parent overlays while confirming CJK input.                                                                                                                                  |
| pointer dismiss focus                      | outside pointer keeps target focus                                                                                           | Outside pointer dismissal closes menu without forcing focus back to trigger.                                                                                                                                                                             |
| nested overlay dismiss                     | top layer first                                                                                                              | Escape/outside dismiss resolves from topmost dismissable layer to avoid collapsing parent overlay stacks in one event.                                                                                                                                   |
| dismiss event scope                        | `ownerDocument`-scoped layer listeners                                                                                       | Escape/outside-pointer dismiss responds only to events from the dropdown document root; cross-document events are ignored, and `Ctrl + primary` context-click pointer paths do not dismiss.                                               |

<Canvas of={DropdownStories.NonDismissible} />
<Canvas of={DropdownStories.DismissGuardHooks} />
<Canvas of={DropdownStories.TypeaheadNavigation} />
<Canvas of={DropdownStories.IconItemNaming} />
<Canvas of={DropdownStories.TriggerLabelledByPrecedence} />
<Canvas of={DropdownStories.TypeaheadIgnoresImeComposition} />
<Canvas of={DropdownStories.OutsideDismissFocusTransfer} />
<Canvas of={DropdownStories.EscapeDismissFocusReturn} />
<Canvas of={DropdownStories.EscapePreemptedByGlobalHandler} />
<Canvas of={DropdownStories.EscapeRepeatGuard} />
<Canvas of={DropdownStories.TabDismissToNextControl} />
<Canvas of={DropdownStories.ShiftTabDismissToPreviousControl} />
<Canvas of={DropdownStories.CloseReasonTelemetry} />
<Canvas of={DropdownStories.ModifierArrowNavigationGuard} />
<Canvas of={DropdownStories.KeyboardActivationDedupe} />
<Canvas of={DropdownStories.ControlledOpenContract} />
<Canvas of={DropdownStories.NestedDismissOrder} />
<Canvas of={DropdownStories.NestedOutsideDismissOrder} />
<Canvas of={DropdownStories.SingleActionableShortcutHints} />

## Data

### Tabs

<Canvas of={TabsStories.Default} />
<Controls of={TabsStories.Default} />

| Prop                                  | Type                                                                                                                                                                                | Notes                                                                                                                                                                                                                                                                   |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `items`                               | `TabItem[]`                                                                                                                                                                         | Include `disabled` for unavailable states. Non-text `label` values can provide `items[].ariaLabel` or `items[].ariaLabelledBy` for explicit tab naming. Development builds warn on duplicate keys, and duplicate render keys are auto-suffixed by item index to avoid React key-collision remount churn. |
| `items[].ariaLabel` / `items[].ariaLabelledBy` | `string` / `string`                                                                                                                                                                 | Accessible naming controls for icon/custom tab labels; rich labels that expose readable text or inline `aria-label` are auto-detected, and development builds warn when non-text labels omit both. Blank values are ignored, and when both are provided `ariaLabelledBy` takes precedence over `ariaLabel`. |
| `value` / `defaultValue`              | `string`                                                                                                                                                                            | Controlled and uncontrolled modes are both supported.                                                                                                                                                                                                                   |
| `ariaLabel` / `ariaLabelledBy`        | `string` / `string`                                                                                                                                                                 | Name the tablist with a direct label or an external heading reference for screen-reader context; blank values are ignored (`ariaLabel` falls back to `Tabs`, `ariaLabelledBy` is dropped).                                                                              |
| `orientation`                         | `'horizontal' \| 'vertical'`                                                                                                                                                        | Keyboard map follows orientation (`Left/Right` or `Up/Down`) with shared `Home/End/PageUp/PageDown` behavior.                                                                                                                                                           |
| RTL keyboard parity                   | horizontal ArrowLeft/ArrowRight auto-flip                                                                                                                                           | In `dir="rtl"` containers, horizontal arrow intent is mirrored so navigation follows visual reading direction.                                                                                                                                                          |
| `activationMode`                      | `'automatic' \| 'manual'`                                                                                                                                                           | Automatic mode switches panels on arrow navigation; manual mode requires `Enter`/`Space` (including legacy `Spacebar`) to activate focused tab.                                                                                                                         |
| automatic navigation key hints        | horizontal: `aria-keyshortcuts="Home End PageDown PageUp ArrowLeft ArrowRight"`; vertical: `aria-keyshortcuts="Home End PageDown PageUp ArrowUp ArrowDown"`                         | In automatic mode, enabled tabs expose navigation shortcut metadata aligned with orientation-specific key maps when more than one tab is actionable; disabled tabs and single-actionable-tab groups omit the hint.                                                      |
| manual activation key hints           | horizontal: `aria-keyshortcuts="Enter Space Home End PageDown PageUp ArrowLeft ArrowRight"`; vertical: `aria-keyshortcuts="Enter Space Home End PageDown PageUp ArrowUp ArrowDown"` | In manual mode, enabled tabs expose activation + navigation shortcut metadata aligned with orientation-specific key maps when more than one tab is actionable; disabled tabs and single-actionable-tab groups omit the hint.                                            |
| manual activation repeat guard        | ignores repeated keydown                                                                                                                                                            | Manual-mode activation ignores repeated `Enter`/`Space` (`event.repeat=true`) so long-press does not emit duplicate tab-change events.                                                                                                                                  |
| manual activation pressed feedback    | activation keydown sets pressed, keyup clears                                                                                                                                       | In manual mode, tab triggers expose pressed offset while `Enter`/`Space`/`Spacebar` is held so keyboard activation has the same active affordance as pointer interactions.                                                                                              |
| modifier-key guard                    | ignores `Ctrl/Meta/Alt + Arrow/Home/End/PageUp/PageDown/Enter/Space`                                                                                                                | Preserves host app/browser modified-key shortcuts and avoids unexpected tab navigation/activation hijack across horizontal (`ArrowLeft/ArrowRight`) and vertical (`ArrowUp/ArrowDown`) maps; `Shift + Enter/Space` remains valid for manual activation flows.           |
| IME activation guard                  | ignores `Enter/Space` while composition is active (`isComposing` or legacy `keyCode=229`)                                                                                           | Prevents accidental manual-tab activation while users confirm CJK input, including legacy browser paths that only emit `keyCode=229`.                                                                                                                                   |
| focus intent policy                   | primary pointer clears focus-visible                                                                                                                                                | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep keyboard-intent ring state; plain primary pointer down clears focus-visible and drives pressed offset lifecycle.                                                                    |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners                                                                                                                                        | When `:focus-visible` matching is unavailable, keyboard re-entry (for example `Tab`/`Shift+Tab` from neighboring controls) still restores active-tab focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| `loop`                                | `boolean`                                                                                                                                                                           | Defaults to `true`; set `false` to stop Arrow navigation at first/last enabled tab instead of wrapping.                                                                                                                                                                 |
| `onValueChange`                       | `(value: string) => void`                                                                                                                                                           | Fires only for enabled tabs and only when active key changes.                                                                                                                                                                                                           |
| controlled-value diagnostics          | dev warning on missing/disabled `value`                                                                                                                                             | Controlled mode falls back to first enabled tab when incoming `value` is missing/disabled, and emits a development warning to surface upstream state drift early.                                                                                                       |
| tab relation                          | mounted `tabpanel` set + `hidden` toggle                                                                                                                                            | Keeps every `aria-controls` target valid while showing only active content panel.                                                                                                                                                                                       |
| tab/panel id strategy                 | key-derived + URL-encoded segment                                                                                                                                                    | `id`/`aria-controls`/`aria-labelledby` wiring derives from `items[].key` (with safe URL-style encoding), so special-character keys stay valid and id refs remain stable when item order changes.                                                                       |

<Canvas of={TabsStories.KeyboardNavigationGuide} />
<Canvas of={TabsStories.NoLoopNavigation} />
<Canvas of={TabsStories.SingleActionableTab} />
<Canvas of={TabsStories.Vertical} />
<Canvas of={TabsStories.RtlKeyboardNavigation} />
<Canvas of={TabsStories.LabelledByHeading} />
<Canvas of={TabsStories.IconOnlyLabelledByPrecedence} />
<Canvas of={TabsStories.ManualActivation} />
<Canvas of={TabsStories.ManualActivationPressedFeedback} />
<Canvas of={TabsStories.ManualFocusResetOnBlur} />
<Canvas of={TabsStories.ManualVerticalActivation} />
<Canvas of={TabsStories.ImeCompositionGuard} />
<Canvas of={TabsStories.PrimaryPointerOnlyPressedState} />
<Canvas of={TabsStories.KeyboardFocusRingShiftTabReentry} />
<Canvas of={TabsStories.ChangeTelemetry} />
<Canvas of={TabsStories.ControlsReferenceMountedPanels} />

### Table

<Canvas of={TableStories.ReleaseChecklist} />

| Prop                                       | Type                                                                                      | Notes                                                                                                                                                                                                                                                                                         |
| ------------------------------------------ | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `columns`                                  | `TableColumn<T>[]`                                                                        | `sortable` + optional `sortAccessor` for stable sorting behavior.                                                                                                                                                                                                                             |
| `columns[].key`                            | `keyof T \| string`                                                                       | Must stay unique per column; development builds warn on duplicate keys and duplicate render keys are auto-suffixed by column index to keep header/cell rendering stable.                                                                                                                       |
| `columns[].sortLabel` / `columns[].sortLabelledBy` | `string` / `string`                                                                       | Sortable-header naming controls for non-readable headers. `sortLabel` defines explicit narration text; `sortLabelledBy` can bind to an external visible heading. Blank values are ignored, and when both are provided, `sortLabelledBy` takes precedence for button naming.                |
| `columns[].rowHeader`                      | `boolean`                                                                                 | Render row identity cells as `<th scope="row">` for better screen-reader row context.                                                                                                                                                                                                         |
| `columns[].render`                         | `(row, rowIndex, sourceIndex) => ReactNode`                                               | `rowIndex` is sorted/visual position; `sourceIndex` preserves original data position.                                                                                                                                                                                                         |
| `data`                                     | `T[]`                                                                                     | Empty arrays render `emptyContent`; ignored while `loading` is true.                                                                                                                                                                                                                          |
| `rowKey`                                   | `(row, rowIndex) => string`                                                               | Recommended stable row identity; `rowIndex` is the source-data index (not sorted visual index), so key derivation stays stable across sorting. `rowKey` resolution is cached by source-data order and reused during sort-only rerenders. Development builds warn when `rowKey` produces duplicate values, and duplicate outputs are auto-suffixed by source index during rendering to avoid key collisions. |
| `caption` / `ariaLabel` / `ariaLabelledBy` | `ReactNode` / `string` / `string`                                                         | Prefer explicit naming via visible caption, screen-reader label, or existing heading binding (`ariaLabelledBy` takes precedence over `ariaLabel`); blank naming values are ignored, and when all naming sources are omitted, table falls back to `aria-label="Data table"`.                   |
| `loading` / `loadingContent`               | `boolean` / `ReactNode`                                                                   | Sets `aria-busy` and renders loading row content while temporarily suppressing data/empty rows and sort interactions.                                                                                                                                                                         |
| scroll container keyboard access           | auto `tabIndex=0` when no actionable sort controls                                        | Horizontal overflow container stays keyboard-focusable when headers are non-sortable or sorting is disabled, preserving non-pointer access to overflowed columns.                                                                                                                             |
| `emptyContent`                             | `ReactNode`                                                                               | Empty rows render as polite status narration (`role="status"`) for screen-reader parity in data-refresh workflows.                                                                                                                                                                            |
| `defaultSortKey` / `defaultSortDirection`  | `string` / `'asc' \| 'desc'`                                                              | Sets initial data order; sorted semantics (`aria-sort` + live status) activate only when row count is greater than one.                                                                                                                                                                       |
| `getSortAriaLabel`                         | `({ columnKey, columnHeader, nextDirection }) => string`                                  | Localize sortable-header aria labels without changing sorting behavior; blank return values are ignored and fallback to default `"{columnHeader} sort {nextDirection}"` narration.                                                                                                            |
| `getSortStatusText`                        | `({ columnKey, columnHeader, direction }) => string`                                      | Customize live-region narration for active sort state changes; defaults to `Sorted by ...`.                                                                                                                                                                                                   |
| `onSortChange`                             | `(key, direction) => void`                                                                | Useful for telemetry and analytics.                                                                                                                                                                                                                                                           |
| sort interaction guard                     | loading/empty/single-row auto-disabled                                                    | Sort buttons disable automatically when sorting would be no-op (`loading`, zero rows, or one row), and no sorted-state narration is emitted in those branches.                                                                                                                                |
| sortable header keyboard                   | `Enter` / `Space` / legacy `Spacebar`                                                     | Sort buttons support pointer + keyboard activation with the same direction toggling semantics across browser key-value variants.                                                                                                                                                              |
| sortable header focus navigation           | `ArrowLeft` / `ArrowRight` / `Home` / `End` / `PageUp` / `PageDown`                       | When sorting controls are actionable, keyboard users can step to previous/next sortable header (`ArrowLeft/ArrowRight` and `PageUp/PageDown`) or jump to first/last sortable header (`Home/End`) without triggering sort state changes. Arrow traversal follows visual direction (RTL-aware). |
| sortable header key hints                  | `aria-keyshortcuts="Enter Space [Home End PageDown PageUp ArrowLeft ArrowRight]"`         | Enabled sort buttons always expose activation hints; focus-navigation hints are appended only when more than one sortable header is actionable, and disabled controls omit the hint.                                                                                                          |
| modifier-key guard                         | ignores `Ctrl/Meta/Alt + ArrowLeft/ArrowRight/Home/End/PageUp/PageDown/Enter/Space`       | Preserves host app/browser modified-key shortcuts and prevents accidental focus jumps/sort toggles from combined key commands in both LTR and RTL mirrored arrow flows; `Shift + Enter/Space` remains valid sortable-header activation.                                                       |
| IME activation guard                       | ignores `Enter/Space` while composition is active (`isComposing` or legacy `keyCode=229`) | Prevents accidental sort toggles while users confirm CJK input, including legacy browser paths that only emit `keyCode=229`.                                                                                                                                                                  |
| sort-button focus intent policy            | primary pointer clears focus-visible                                                      | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep keyboard-intent ring state; plain primary pointer down clears focus-visible and applies pressed offset lifecycle.                                                                                                            |
| document-scoped focus intent fallback      | owner-document `keydown` / pointer listeners                                              | When `:focus-visible` matching is unavailable, keyboard re-entry (for example `Tab`/`Shift+Tab` from neighboring controls) still restores sortable-header focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| keyboard pressed feedback                  | activation keydown sets pressed, keyup clears                                             | Sort buttons expose active-state offset during `Enter/Space` key hold so keyboard users receive the same press affordance as pointer users.                                                                                                                                                   |
| activation repeat guard                    | ignores repeated keydown                                                                  | Holding sort activation keys (`Enter` / `Space`, `event.repeat=true`) does not trigger repeated sort toggles, preventing telemetry jitter and accidental direction flips.                                                                                                                     |
| keyboard-click dedupe                      | ignores synthesized keyboard-origin click (`detail=0`) after handled activation keydown   | Prevents duplicate sort toggles/telemetry when environments emit both keydown and follow-up click for the same keyboard action.                                                                                                                                                               |
| `aria-sort` exposure                       | active sorted header only                                                                 | Unsorted headers (including sortable ones) omit `aria-sort`, reducing screen-reader noise and matching ARIA table guidance.                                                                                                                                                                   |
| loading sort semantics                     | clears `aria-sort` during `loading`                                                       | Loading mode suppresses stale sort semantics and leaves `aria-sort` unset until data rows are interactive again.                                                                                                                                                                              |

<Canvas of={TableStories.SortTelemetry} />
<Canvas of={TableStories.RtlArrowNavigation} />
<Canvas of={TableStories.LocalizedSortLabels} />
<Canvas of={TableStories.SortLabelForCustomHeader} />
<Canvas of={TableStories.RichTextHeaderAutoSortLabel} />
<Canvas of={TableStories.AriaLabelHeaderAutoSortLabel} />
<Canvas of={TableStories.SortLabelledByPrecedence} />
<Canvas of={TableStories.LoadingState} />
<Canvas of={TableStories.EmptyState} />
<Canvas of={TableStories.SingleRowSortDisabled} />
<Canvas of={TableStories.SingleSortableColumnShortcutHints} />
<Canvas of={TableStories.PrimaryPointerOnlySortPress} />
<Canvas of={TableStories.KeyboardFocusRingShiftTabReentry} />
<Canvas of={TableStories.KeyboardReachableScrollContainer} />
<Canvas of={TableStories.WithRowAction} />
<Canvas of={TableStories.InvalidDefaultSortKeyFallback} />
<Canvas of={TableStories.SourceIndexRowKeyStability} />
<Canvas of={TableStories.RenderIndexSemantics} />
<Canvas of={TableStories.AccessibleNameWithoutCaption} />
<Canvas of={TableStories.AccessibleNameFallback} />
<Canvas of={TableStories.AccessibleNameLabelledByHeading} />
<Canvas of={TableStories.RowHeaderSemantics} />

### Pagination

<Canvas of={PaginationStories.Controlled} />
<Controls of={PaginationStories.KeyboardShortcuts} />

| Prop                                  | Type                                                                    | Notes                                                                                                                                                                                                          |
| ------------------------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page`                                | `number`                                                                | Current page index (1-based).                                                                                                                                                                                  |
| `pageCount`                           | `number`                                                                | Total number of pages; values `<= 1` render no UI.                                                                                                                                                             |
| `onPageChange`                        | `(page: number) => void`                                                | Called for navigation intent.                                                                                                                                                                                  |
| `showFirstLast`                       | `boolean`                                                               | Toggle first/last jump buttons.                                                                                                                                                                                |
| `siblingCount` / `boundaryCount`      | `number`                                                                | Controls visible number tokens before ellipsis.                                                                                                                                                                |
| `ariaLabel` / `ariaLabelledBy`        | `string` / `string`                                                     | Name pagination nav with direct copy or an external heading reference; `ariaLabelledBy` takes precedence when both are provided, and blank naming values are ignored (`ariaLabel` falls back to `Pagination`). |
| `getItemAriaLabel`                    | `(type, page) => string`                                                | Customize pagination control narration for i18n; boundary `previous/next` pages are clamped to valid range, and blank return values fallback to default narration.                                             |
| RTL keyboard parity                   | ArrowLeft/ArrowRight auto-flip                                          | In `dir="rtl"` containers, horizontal arrow shortcuts mirror navigation intent to match visual flow.                                                                                                           |
| modifier-key guard                    | ignores `Ctrl/Meta/Alt + Home/End/ArrowLeft/ArrowRight`                 | Preserves host app/browser modified-key shortcuts and avoids accidental page jumps under combined shortcut chords; `Shift`-modified shortcuts remain valid pagination navigation.                              |
| keyboard shortcut hints               | `aria-keyshortcuts="[Home] [End] ArrowLeft ArrowRight"` on enabled controls | Announces pagination keyboard navigation affordances to assistive tech and QA audits; controls omit unavailable boundary keys (`Home` on first page, `End` on last page), and disabled controls omit shortcut hints.                                   |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners                            | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores pagination button focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |

<Canvas of={PaginationStories.KeyboardShortcuts} />
<Canvas of={PaginationStories.ModifierKeyGuard} />
<Canvas of={PaginationStories.RtlKeyboardShortcuts} />
<Canvas of={PaginationStories.BoundaryAriaLabels} />
<Canvas of={PaginationStories.BoundaryShortcutHints} />
<Canvas of={PaginationStories.LabelledByHeading} />
<Canvas of={PaginationStories.FocusIntentReentry} />

## Feedback

### Empty

<Canvas of={EmptyStories.Default} />
<Controls of={EmptyStories.WithAction} />

| Prop                    | Type                                           | Notes                                                |
| ----------------------- | ---------------------------------------------- | ---------------------------------------------------- |
| `title` / `description` | `ReactNode`                                    | Empty-state headline and helper explanation text.    |
| `icon` / `action`       | `ReactNode`                                    | Optional visual cue and primary call-to-action slot. |
| `tone`                  | `'default' \| 'info' \| 'warning' \| 'danger'` | Semantic emphasis for empty-state severity context.  |
| `align` / `titleAs`     | `'center' \| 'left'` / heading element         | Control content layout and heading semantics.        |

<Canvas of={EmptyStories.ToneMatrix} />

### Skeleton

<Canvas of={SkeletonStories.Default} />
<Controls of={SkeletonStories.NarratedStatus} />

| Prop                          | Type                                                       | Notes                                                                                                                    |
| ----------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `variant`                     | `'rect' \| 'text' \| 'circle'`                             | Shape selection for block/text/avatar placeholders.                                                                      |
| `animated`                    | `boolean`                                                  | Toggle shimmer animation for reduced-motion or static placeholders.                                                      |
| reduced-motion fallback       | shimmer tagged with `data-aurora-reduced-motion="animate"` | With `GlobalStyles`, shimmer auto-disables under `prefers-reduced-motion` while placeholder geometry stays stable.       |
| `ariaLabel` / `ariaLabelledBy` | `string` / `string`                                      | Enables narrated loading status semantics when needed. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. |
| `width` / `height` / `radius` | `number \| string`                                         | Explicit geometry control for layout matching.                                                                           |

<Canvas of={SkeletonStories.ProfileCard} />
<Canvas of={SkeletonStories.LabelledByPrecedence} />

### LoadingDots

<Canvas of={LoadingDotsStories.Default} />
<Controls of={LoadingDotsStories.SlowPulse} />

| Prop                    | Type                                          | Notes                                                                                                          |
| ----------------------- | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `running`               | `boolean`                                     | Pause/resume the animated dots.                                                                                |
| `interval` / `dotCount` | `number`                                      | Controls animation rhythm and max visible dots.                                                                |
| `respectReducedMotion`  | `boolean`                                     | Defaults to `true`; when OS reduced-motion is enabled, dots stay static while loading semantics remain intact. |
| `label` / `live`        | `string` / `'polite' \| 'assertive' \| 'off'` | Screen-reader narration and live-region urgency; blank `label` values are ignored and fallback to `Loading`.   |

<Canvas of={LoadingDotsStories.Paused} />

### StreamingText

<Canvas of={StreamingTextStories.Default} />
<Controls of={StreamingTextStories.InstantDone} />

| Prop                                | Type                 | Notes                                                                                                                        |
| ----------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `streaming`                         | `boolean`            | Toggle realtime token streaming vs instant completion mode.                                                                  |
| `speed` / `startDelay`              | `number`             | Controls token cadence and delayed start.                                                                                    |
| `cursorChar` / `showCursorWhenDone` | `string` / `boolean` | Cursor visual customization and completion behavior.                                                                         |
| `respectReducedMotion`              | `boolean`            | Defaults to `true`; reduced-motion users see immediate final text and completion callbacks without token-by-token animation. |
| `label`                             | `string`             | Accessible narration label for streaming status; blank values are ignored and fallback to `Streaming text`.                  |
| `onProgress` / `onComplete`         | callbacks            | Lifecycle hooks for telemetry and completion flow.                                                                           |

<Canvas of={StreamingTextStories.MultilineCursor} />

### Progress

<Canvas of={ProgressStories.Default} />
<Controls of={ProgressStories.Default} />

| Prop                    | Type                                                               | Notes                                                                                                                               |
| ----------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| `value` / `min` / `max` | `number`                                                           | Determinate range with safe clamp and percent narration.                                                                            |
| `indeterminate`         | `boolean`                                                          | Removes `aria-valuenow` and switches to loading animation branch.                                                                   |
| reduced-motion fallback | indicator tagged with `data-aurora-reduced-motion` tokens          | Indeterminate animation and determinate width transition auto-reduce under `prefers-reduced-motion` when `GlobalStyles` is mounted. |
| `label` / `valueText`   | `string`                                                           | Explicit screen-reader label and override value narration text; blank `label` values are ignored and fallback to `Progress`.        |
| `tone` / `size`         | `'default' \| 'success' \| 'warning' \| 'danger'` / `'sm' \| 'md'` | Visual emphasis and bar density controls.                                                                                           |
| `showValueLabel`        | `boolean`                                                          | Renders helper value text under the progress track.                                                                                 |

<Canvas of={ProgressStories.Indeterminate} />

### Alert

<Canvas of={AlertStories.Default} />
<Controls of={AlertStories.Dismissible} />

| Prop                                  | Type                                                                                    | Notes                                                                                                                                                                                                        |
| ------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `tone`                                | `'info' \| 'success' \| 'warning' \| 'danger'`                                          | Controls visual emphasis and live-region urgency.                                                                                                                                                            |
| `description`                         | `ReactNode`                                                                             | Main alert body copy under title.                                                                                                                                                                            |
| `onClose`                             | `() => void`                                                                            | Enables dismiss button branch for actionable notices.                                                                                                                                                        |
| `closeLabel`                          | `string`                                                                                | Localizable dismiss-button accessible name; blank values are ignored and fallback to `Dismiss alert`.                                                                                                        |
| close-button keyboard hints           | `aria-keyshortcuts="Enter Space"`                                                       | Dismiss button exposes keyboard shortcut metadata for QA/AT discoverability.                                                                                                                                 |
| close-button pressed feedback         | primary pointer + unmodified `Enter`/`Space` (+ `Space` / legacy `Spacebar` key values) | Dismiss button exposes pressed-state offset/background for pointer and keyboard activation keys while ignoring `Ctrl/Meta/Alt` chords and IME composition key paths (`isComposing` / `keyCode=229`) to preserve host shortcuts and prevent false pressed feedback during CJK confirmation. |
| close-button focus intent             | primary pointer clears focus-visible                                                    | Non-primary dismiss-button pointer interactions plus `Ctrl + primary click` context-click paths keep keyboard-intent ring state to avoid accidental focus-ring loss.                                           |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners                                            | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores alert close-button focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| `ariaLabel` / `ariaLabelledBy`        | `string` / `string`                                                                     | Optional status-region naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback.                                                                                                           |
| `live`                                | `'polite' \| 'assertive' \| 'off'`                                                      | Overrides default aria-live behavior.                                                                                                                                                                        |

<Canvas of={AlertStories.AccessibilityMetadata} />
<Canvas of={AlertStories.LabelledByPrecedence} />
<Canvas of={AlertStories.FocusIntentReentry} />
<Canvas of={AlertStories.CloseButtonKeyboardPressedState} />

### Toast

<Canvas of={ToastStories.Default} />
<Controls of={ToastStories.Default} />

| Prop                                   | Type                                                                                                      | Notes                                                                                                                                                                                                                                                                               |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tone`                                 | `'info' \| 'success' \| 'warning' \| 'danger'`                                                            | Drives semantics: non-action toasts use `status/alert`; actionable toasts use `dialog/alertdialog` by tone.                                                                                                                                                                         |
| `duration`                             | `number`                                                                                                  | Auto-dismiss delay in ms; `0` disables timer. If `action` is provided and `duration` is omitted, timer defaults to persistent (`0`) to avoid expiring actionable notices.                                                                                                           |
| `live`                                 | `'polite' \| 'assertive' \| 'off'`                                                                        | Optional live-region politeness override for notification noise control.                                                                                                                                                                                                            |
| `pauseOnHover`                         | `boolean`                                                                                                 | Suspends timer while hovered or while focus stays inside toast controls. Auto-dismiss also pauses when the document tab is hidden and resumes with remaining time when visible again.                                                                                               |
| timer document scope                   | `ownerDocument.defaultView` + visibility listeners                                                        | Auto-dismiss start/pause/resume/cleanup uses the toast root document window so iframe/multi-document hosts keep timer behavior isolated per document context.                                                                                                                       |
| `position`                             | `'bottom-right' \| 'bottom-left' \| 'top-right' \| 'top-left'`                                            | Controls viewport anchor for stack placement and Escape-priority grouping.                                                                                                                                                                                                          |
| `closeOnEscape`                        | `boolean`                                                                                                 | Optional keyboard dismiss path; Escape is consumed so stacked toasts dismiss one at a time.                                                                                                                                                                                         |
| dismiss key hints                      | `aria-keyshortcuts="Escape"` (top escapable toast only)                                                   | In stacked layouts, only the current top escapable toast exposes Escape shortcut metadata so QA/AT tooling is not misled by pinned/non-closeable notifications.                                                                                                                     |
| Escape repeat guard                    | ignores repeated keydown                                                                                  | Escape long-press (`event.repeat=true`) is ignored so one key press closes at most one toast and avoids duplicate close telemetry.                                                                                                                                                  |
| modifier-key guard                     | ignores `Ctrl/Meta/Alt + Escape`                                                                          | Preserves host app/browser modified-key shortcuts and prevents accidental toast dismissal or hook side effects under combined key chords, including stacked-toast queues.                                                                                                           |
| Shift+Escape parity                    | `Shift + Escape` remains dismissable                                                                      | Only modified shortcuts with `Ctrl/Meta/Alt` are filtered; `Shift + Escape` still triggers standard toast dismiss and `escape-key` telemetry.                                                                                                                                       |
| dynamic Escape-hint sync               | runtime `closeOnEscape` changes recompute stack hints                                                     | When stacked toast `closeOnEscape` policy changes at runtime, shortcut metadata reassigns to the current top escapable toast without stale hints.                                                                                                                                   |
| `onEscapeKeyDown`                      | `(event: KeyboardEvent) => void`                                                                          | Optional toast-level Escape hook; call `event.preventDefault()` to keep toast open in guarded flows.                                                                                                                                                                                |
| actionable role mapping                | `action` => `dialog/alertdialog`                                                                          | When `action` is provided, toast exposes interactive surface semantics (`aria-modal="false"`) instead of live-region-only status roles.                                                                                                                                             |
| `onCloseReason`                        | `(reason: 'close-button' \| 'escape-key' \| 'timeout') => void`                                           | Emits a structured close reason for analytics and workflow telemetry.                                                                                                                                                                                                               |
| close callback order                   | `onCloseReason` -> `onClose` -> `onOpenChange(false)`                                                     | Close lifecycle is deterministic across button/Escape/timeout paths, so telemetry and state reducers can avoid duplicate side effects.                                                                                                                                              |
| IME safety                             | Escape composition guard                                                                                  | Escape dismissal ignores active IME composition (`isComposing`) and legacy fallback paths that only expose `keyCode=229`, to avoid accidental close while confirming CJK text input.                                                                                                |
| Escape priority                        | preempted events skip toast hook                                                                          | If higher-level handlers already `preventDefault()` Escape, toast does not run `onEscapeKeyDown` or close.                                                                                                                                                                          |
| overlay-layer preemption               | top dismissable overlay consumes first Escape                                                             | When a higher overlay layer handles Escape and marks the event as preempted, toast remains open until the next Escape after that overlay closes.                                                                                                                                    |
| stacked Escape order                   | top-priority toast first                                                                                  | In multi-toast layouts, Escape dismisses the top toast (latest opened or most recently focused/hovered).                                                                                                                                                                            |
| Escape scope isolation                 | `ownerDocument` stack isolation                                                                           | Escape-priority stacks and shortcut hints are isolated per document root, so key events in another document do not close this toast stack.                                                                                                                                          |
| stacked viewport layout                | per-position visual offset                                                                                | Same-corner toast stacks apply deterministic offset so older notifications remain visible under the latest toast.                                                                                                                                                                   |
| close-button focus intent              | primary pointer clears focus-visible                                                                      | Non-primary close-button pointer interactions plus `Ctrl + primary click` context-click paths keep keyboard-intent ring state to avoid accidental focus-ring loss.                                                                                                  |
| document-scoped focus intent fallback  | owner-document `keydown` / pointer listeners                                                              | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from a preceding control) still restores toast close-button focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |
| close-button keyboard pressed feedback | unmodified non-repeat `Enter`/`Space` (+ `Space` / legacy `Spacebar` key values) + `aria-keyshortcuts="Enter Space"` | Close button exposes pressed-state offset for unmodified keyboard activation keys and explicit shortcut hints; repeated keydown, `Ctrl/Meta/Alt` chords, and IME composition key paths (`isComposing` / `keyCode=229`) are ignored so host shortcuts, long-press, and CJK confirmation do not trigger false pressed visuals. |
| `closeLabel`                           | `string`                                                                                                  | Customize close-button accessible name for i18n; blank values are ignored and fallback to `Close toast`.                                                                                                                                                                            |
| `ariaLabel` / `ariaLabelledBy`         | `string` / `string`                                                                                       | Optional live-region naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without `ariaLabelledBy`, `ariaLabel` falls back to title-derived naming. |

<Canvas of={ToastStories.ActionRequired} />
<Canvas of={ToastStories.ActionRequiredDanger} />
<Canvas of={ToastStories.CloseReasonTelemetry} />
<Canvas of={ToastStories.EscapeStackOrder} />
<Canvas of={ToastStories.EscapeShortcutSync} />
<Canvas of={ToastStories.StackedViewportOffset} />
<Canvas of={ToastStories.FocusedToastEscapesFirst} />
<Canvas of={ToastStories.CloseButtonPrimaryPointerOnly} />
<Canvas of={ToastStories.CloseButtonKeyboardPressedState} />
<Canvas of={ToastStories.FocusIntentReentry} />
<Canvas of={ToastStories.EscapePreemptedByGlobalHandler} />
<Canvas of={ToastStories.EscapePreemptedByOverlayLayer} />
<Canvas of={ToastStories.EscapePreemptedSkipsToastHook} />
<Canvas of={ToastStories.EscapeGuardedByToastHandler} />
<Canvas of={ToastStories.EscapeIgnoresImeComposition} />
<Canvas of={ToastStories.LabelledByPrecedence} />
<Canvas of={ToastStories.LiveRegionOff} />

## AI

### CommandPalette

<Canvas of={CommandPaletteStories.SearchCommands} />
<Controls of={CommandPaletteStories.SearchCommands} />

| Prop                                       | Type                                                                                                                  | Notes                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `commands`                                 | `CommandItem[]`                                                                                                       | `disabled` commands remain visible but non-selectable. Rich labels are searchable via readable text extraction (including inline `aria-label`); non-readable labels should provide `textValue` aliases. Development builds warn when command keys are duplicated, warn when non-text labels omit accessible naming, and warn when non-text labels omit both `textValue` and searchable `keywords`. Duplicate command keys are auto-suffixed in rendered option keys to avoid React key-collision remount churn. |
| `commands[].ariaLabel`                     | `string`                                                                                                              | Accessible name override for icon/non-text command options; rich labels with inline `aria-label` are auto-detected before warning, and blank values are ignored to avoid overriding visible option naming.                                                                                                                                                                                         |
| `title` / `description`                    | `ReactNode`                                                                                                           | Localizable dialog heading and supporting copy for command context.                                                                                                                                                                                                                                                                                                                                |
| `ariaLabel` / `ariaLabelledBy`             | `string` / `string`                                                                                                   | Optional command-palette dialog naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without overrides, naming falls back to rendered `title`.                                                                                                                                                              |
| `searchAriaLabel`                          | `string`                                                                                                              | Localizable accessible name for the search combobox; blank values are ignored and fallback to `Search commands`.                                                                                                                                                                                                                                                                                   |
| `placeholder`                              | `string`                                                                                                              | Custom search input hint text for product-specific command vocabulary.                                                                                                                                                                                                                                                                                                                             |
| `loading` / `loadingContent`               | `boolean` / `ReactNode`                                                                                               | Loading mode surfaces pending copy, sets combobox `aria-busy`, and suppresses listbox mount plus command-navigation/selection interactions until command data is ready.                                                                                                                                                                                                                            |
| `closeLabel`                               | `string`                                                                                                              | Localizable close-button accessible name; blank values are ignored and fallback to `Close dialog`.                                                                                                                                                                                                                                                                                                 |
| dismiss key hints                          | `aria-keyshortcuts="Escape"` when `closeOnEscape` is enabled                                                          | Inherited from dialog dismiss policy so QA/AT tooling can discover Escape dismiss affordance only when that behavior is actionable.                                                                                                                                                                                                                                                                |
| close-button key hints                     | `aria-keyshortcuts="Enter Space"`                                                                                     | Inherited from dialog close affordance so QA/AT tooling can discover deterministic keyboard dismiss activation keys.                                                                                                                                                                                                                                                                               |
| close-button focus fallback                | owner-document `keydown` / pointer intent tracking                                                                    | Inherited from dialog close affordance so `:focus-visible` fallback still restores close-button focus ring on keyboard re-entry (for example `Shift+Tab` from the search input), including iframe hosts.                                                                                                                                                                                           |
| `closeOnSelect`                            | `boolean`                                                                                                             | Defaults to `true`; set `false` for batch workflows that keep the palette open after each command.                                                                                                                                                                                                                                                                                                 |
| `clearQueryOnEscape`                       | `boolean`                                                                                                             | Defaults to `true`; first Escape clears active query before dismiss is allowed, reducing accidental close during search refinement.                                                                                                                                                                                                                                                                |
| `closeOnEscape` / `closeOnOutsidePointer`  | `boolean`                                                                                                             | Controls whether overlay dismisses on Escape or outside pointer interactions for blocking approval flows.                                                                                                                                                                                                                                                                                          |
| dismiss event scope                        | `ownerDocument`-scoped layer listeners                                                                                | Escape/outside-pointer dismissal only responds to events from the palette's own document root; cross-document events are ignored to prevent accidental closes in iframe/multi-root hosts.                                                                                                                                                                                                          |
| nested overlay dismiss                     | top layer first                                                                                                       | When nested inside another dismissable surface, first Escape/outside interaction closes the command palette layer and preserves parent overlay state until the next dismiss action.                                                                                                                                                                                                                |
| `onEscapeKeyDown` / `onPointerDownOutside` | `(event) => void`                                                                                                     | Low-level dismiss hooks; call `event.preventDefault()` for conditional approval guards while keeping default close policy enabled.                                                                                                                                                                                                                                                                 |
| `emptyMessage`                             | `ReactNode`                                                                                                           | Custom empty-state guidance for no-match queries.                                                                                                                                                                                                                                                                                                                                                  |
| `resultsAriaLabel`                         | `string`                                                                                                              | Localized accessible name for the command result listbox; blank values are ignored and fallback to `Command results`.                                                                                                                                                                                                                                                                              |
| `onQueryChange`                            | `(query: string) => void`                                                                                             | Use for analytics and AI telemetry pipelines; palette close now emits `""` so external telemetry/state can reset with internal query lifecycle.                                                                                                                                                                                                                                                    |
| `onCloseReason`                            | `(reason: 'close-button' \| 'item-select' \| 'escape-key' \| 'outside-pointer') => void`                              | Emits structured close telemetry so analytics can distinguish user-dismiss path vs action completion.                                                                                                                                                                                                                                                                                              |
| close lifecycle order                      | `item-select: onSelect -> onCloseReason -> onOpenChange(false)`; close-button: `onCloseReason -> onOpenChange(false)` | Keep analytics/actions deterministic and avoid duplicate side effects across callbacks.                                                                                                                                                                                                                                                                                                            |
| `getResultsStatusText`                     | `({ query, visibleCount, enabledCount, totalCount }) => string`                                                       | Customize live narration with both visible and actionable-result counts.                                                                                                                                                                                                                                                                                                                           |
| `onOpenChange`                             | `(open: boolean) => void`                                                                                             | Required controlled dialog state.                                                                                                                                                                                                                                                                                                                                                                  |
| combobox relation                          | conditional `aria-controls` + listbox mount                                                                           | Search input references listbox only while filtered results exist, avoiding empty-state dangling IDs.                                                                                                                                                                                                                                                                                              |
| focus model                                | combobox + `aria-activedescendant`                                                                                    | Options stay outside tab order to keep keyboard flow anchored on the search input.                                                                                                                                                                                                                                                                                                                 |
| activedescendant validity                  | enabled option only                                                                                                   | Active option references are sanitized during command/query updates so combobox never points to missing or disabled options, and `aria-activedescendant` is cleared when filtered results are empty.                                                                                                                                                                                               |
| search key hints                           | `aria-keyshortcuts="[ArrowDown ArrowUp Home End PageDown PageUp] [Enter] [Escape]"`                                   | Combobox exposes state-aware shortcut metadata for QA/AT discoverability: navigation hints appear only when more than one command is actionable, `Enter` appears when at least one command is actionable, and `Escape` is omitted when both `closeOnEscape` and `clearQueryOnEscape` are disabled.                                                                                                  |
| Enter repeat guard                         | ignores repeated keydown                                                                                              | Command execution ignores repeated `Enter` (`event.repeat=true`) so long-press does not trigger duplicate actions or telemetry noise.                                                                                                                                                                                                                                                              |
| Escape repeat guard                        | ignores repeated keydown                                                                                              | Repeated `Escape` (`event.repeat=true`) is ignored for both query-clear and dismiss paths so long-press does not trigger accidental close or duplicate telemetry.                                                                                                                                                                                                                                  |
| option activation guard                    | ignores `Ctrl/Meta/Alt + Enter/Space`, repeated keydown, and `Ctrl + primary mousedown`                              | Listbox option activation stays scoped to unmodified, non-repeat keys, and option `mousedown` focus-guarding now skips `Ctrl + primary click` so host shortcuts/context-click flows are not treated as standard primary activation.                                                                                                                                                              |
| option key hints                           | actionable options expose `aria-keyshortcuts="Enter Space"`                                                           | Command options expose activation shortcuts for QA/AT discoverability, while disabled options omit key hints to avoid false affordance metadata.                                                                                                                                                                                                                                                   |
| long-list keyboard jump                    | `PageUp` / `PageDown`                                                                                                 | Page keys jump by 5 actionable commands while skipping disabled options, reducing Arrow-key repetition in dense command sets.                                                                                                                                                                                                                                                                      |
| modifier-key guard                         | ignores `Ctrl/Meta/Alt + ArrowDown/ArrowUp/Home/End/Page/Enter/Escape` command handling                               | Preserves native input editing shortcuts and avoids hijacking app-level modified key flows during search/dismiss; `Shift + Escape` remains available for query-clear/dismiss progression.                                                                                                                                                                                                          |
| search matching                            | accent-insensitive + lowercase normalization                                                                          | Plain query text like `cafe` can match `Café` labels/aliases.                                                                                                                                                                                                                                                                                                                                      |
| IME safety                                 | composition key guard                                                                                                 | Arrow/Enter command handling and query-clearing Escape behavior are ignored while `isComposing` and legacy fallback paths that only expose `keyCode=229`, so CJK composition confirmation does not execute commands or clear active query.                                                                                                                                                         |
| Escape priority                            | preempted events skip palette hook/dismiss                                                                            | If higher-level handlers already `preventDefault()` Escape, palette keeps dialog open and does not fire `onEscapeKeyDown`.                                                                                                                                                                                                                                                                         |

<Canvas of={CommandPaletteStories.QueryTelemetry} />
<Canvas of={CommandPaletteStories.LoadingState} />
<Canvas of={CommandPaletteStories.CloseReasonTelemetry} />
<Canvas of={CommandPaletteStories.CloseButtonFocusIntentReentry} />
<Canvas of={CommandPaletteStories.NestedDismissOrder} />
<Canvas of={CommandPaletteStories.EmptyStateAriaControlsLifecycle} />
<Canvas of={CommandPaletteStories.LocalizedResultsLabel} />
<Canvas of={CommandPaletteStories.LocalizedDialogCopy} />
<Canvas of={CommandPaletteStories.LabelledByPrecedence} />
<Canvas of={CommandPaletteStories.DisabledCommandGuard} />
<Canvas of={CommandPaletteStories.SingleActionableShortcutHints} />
<Canvas of={CommandPaletteStories.TextValueAndAccentSearch} />
<Canvas of={CommandPaletteStories.IconOnlyCommandNaming} />
<Canvas of={CommandPaletteStories.PersistentSelection} />
<Canvas of={CommandPaletteStories.OptionActivationKeyGuard} />
<Canvas of={CommandPaletteStories.RefinedSearchKeepsActiveCommand} />
<Canvas of={CommandPaletteStories.PagedKeyboardNavigation} />
<Canvas of={CommandPaletteStories.ImeCompositionGuard} />
<Canvas of={CommandPaletteStories.EscapePreemptedByGlobalHandler} />
<Canvas of={CommandPaletteStories.NonDismissible} />
<Canvas of={CommandPaletteStories.GuardedDismissEvents} />
<Canvas of={CommandPaletteStories.EscapeClearsQueryFirst} />
<Canvas of={CommandPaletteStories.EscapeRepeatGuard} />

### PromptInput

<Canvas of={PromptInputStories.WithAiRequestState} />
<Controls of={PromptInputStories.Default} />

| Prop                                  | Type                                           | Notes                                                                                                                                                                                                     |
| ------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `onSubmit`                            | `(prompt: string) => void`                     | Triggered by send button or Ctrl/Cmd + Enter shortcut; ignored during IME composition and legacy fallback paths that only expose `keyCode=229`.                                                           |
| `submitting`                          | `boolean`                                      | Locks input + button and updates live hint during AI request execution.                                                                                                                                   |
| `placeholder`                         | `string`                                       | Optional prompt guidance text.                                                                                                                                                                            |
| `ariaLabel` / `ariaLabelledBy`        | `string` / `string`                            | Optional textarea naming controls. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without `ariaLabelledBy`, `ariaLabel` falls back to `Prompt input`. |
| `shortcutHint` / `submittingHint`     | `ReactNode`                                    | Customizable idle/pending helper copy for localization and product voice alignment.                                                                                                                       |
| keyboard metadata                     | idle: `aria-keyshortcuts="Control+Enter Meta+Enter"` | Prompt textarea exposes submit shortcut hints for QA/AT discoverability only while submit action is available; pending state omits shortcut metadata.                                                    |
| helper hint binding                   | `aria-describedby`                             | Prompt textarea links to live helper copy so screen readers announce current shortcut or pending state guidance.                                                                                          |
| focus intent policy                   | primary pointer clears focus-visible           | Non-primary pointer interactions and `Ctrl + primary click` context-click paths keep prompt textarea focus-visible state; plain primary pointer clears keyboard-intent focus-visible feedback before submit intent. |
| document-scoped focus intent fallback | owner-document `keydown` / pointer listeners   | When `:focus-visible` matching is unavailable, keyboard re-entry (for example Tabbing from the previous control) still restores prompt textarea focus-visible state consistently, including iframe hosts. Non-primary pointer buttons and `Ctrl + primary click` do not clear fallback keyboard intent. |

<Canvas of={PromptInputStories.LocalizedCopy} />
<Canvas of={PromptInputStories.LabelledByPrecedence} />
<Canvas of={PromptInputStories.FocusIntentReentry} />

### MessageBubble

<Canvas of={MessageBubbleStories.Conversation} />
<Controls of={MessageBubbleStories.Default} />

| Prop              | Type                                | Notes                                                                                                                                |
| ----------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `speaker`         | `'user' \| 'assistant' \| 'system'` | Applies alignment and semantic labeling (`aria-label`).                                                                              |
| `children`        | `ReactNode`                         | Message content body.                                                                                                                |
| `speakerLabel`    | `string`                            | Optional localized speaker text used to compose fallback accessible label (`\`${speakerLabel} message\``); blank values are ignored. |
| `ariaLabel` / `ariaLabelledBy` | `string` / `string`              | Explicit naming controls for message bubbles. Blank values are ignored; when both are provided, `ariaLabelledBy` takes precedence and suppresses `ariaLabel` fallback. Without `ariaLabelledBy`, `ariaLabel` falls back to speaker-derived naming. |
| `roleDescription` | `string`                            | Optional localized `aria-roledescription` for message semantics; blank values fallback to `message`.                                 |

<Canvas of={MessageBubbleStories.LocalizedNaming} />
<Canvas of={MessageBubbleStories.LabelledByPrecedence} />

### ReasoningPanel

<Canvas of={ReasoningPanelStories.Default} />
<Controls of={ReasoningPanelStories.Default} />

| Prop                            | Type                              | Notes                                                                                                                |
| ------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `title`                         | `ReactNode`                       | Toggle trigger title.                                                                                                |
| `steps`                         | `string[]`                        | Ordered reasoning steps; empty arrays show fallback state.                                                           |
| `defaultOpen`                   | `boolean`                         | Initial expanded state for diagnostics-oriented views.                                                               |
| `expandLabel` / `collapseLabel` | `string`                          | Localizable button accessible labels for collapsed/expanded states; blank values fallback to default English labels. |
| keyboard metadata               | `aria-keyshortcuts="Enter Space"` | Toggle button exposes disclosure shortcuts for QA/AT discoverability.                                                |
| `listAriaLabel`                 | `string`                          | Accessible name for the reasoning list region; blank values fallback to `Reasoning steps`.                           |
| `emptyText`                     | `ReactNode`                       | Empty-state copy when no reasoning steps are present.                                                                |

<Canvas of={ReasoningPanelStories.LocalizedLabels} />

### StreamingCodeBlock

<Canvas of={StreamingCodeBlockStories.Default} />
<Controls of={StreamingCodeBlockStories.Default} />

| Prop       | Type                               | Notes                                                                                                               |
| ---------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `code`     | `string`                           | Source content to progressively reveal.                                                                             |
| `language` | `string`                           | Header hint for syntax context.                                                                                     |
| `speed`    | `number`                           | Milliseconds per character; `0` renders immediately.                                                                |
| `label`    | `string`                           | Accessible region name for assistive tech context; blank values are ignored and fallback to `Streaming code block`. |
| `live`     | `'polite' \| 'assertive' \| 'off'` | Optional live-region politeness for progressive code narration; defaults to `off`.                                  |
